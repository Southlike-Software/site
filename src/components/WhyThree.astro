---
const perks = [
  "Desenvolvimento personalizado, não templates genéricos",
  "Acesso direto ao fundador, não a intermediários",
  "Influência direta no desenvolvimento da plataforma",
];
---

<section class="mx-auto max-w-7xl px-6 pb-16 pt-16 lg:px-8">
  <div class="mx-auto max-w-3xl text-center">
    <h2 class="text-3xl font-semibold tracking-tight sm:text-4xl">Por que apenas 3 imobiliárias?</h2>
    <p class="mt-3 text-neutral-300">Transparência Total</p>
  </div>
  <div class="mx-auto mt-8 max-w-3xl rounded-lg border border-neutral-800 bg-neutral-900/60 p-6">
    <ul class="space-y-3 text-sm text-neutral-300">
      {perks.map((p) => (
        <li class="flex gap-3">
          <span class="text-purple-400">•</span>
          <span>{p}</span>
        </li>
      ))}
    </ul>

    <div class="mt-6 space-y-2 text-sm text-neutral-300">
      <div>▪️ Vaga 1: Em processo de seleção</div>
      <div>▪️ Vaga 2: Disponível</div>
      <div>▪️ Vaga 3: Disponível</div>
      <div class="text-neutral-500">Última atualização: <span id="vaga-updated"></span></div>
    </div>
  </div>

  <script>
    const el = document.getElementById('vaga-updated');
    if (el) {
      const now = new Date();
      const options = { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' };
      el.textContent = now.toLocaleString('pt-BR', options);
    }
  </script>
</section>

